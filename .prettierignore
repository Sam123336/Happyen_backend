.dart_tool/
.git/
build/
coverage/
dist/
node_modules/
pnpm-lock.yaml

# drizzle-kit rewrites these snapshots on every `db:generate`, in its own
# formatting. Format-checking generated metadata only breaks CI after a
# migration, so they are ignored like the lockfile.
drizzle/meta/


# Copy of the plan of record; kept byte-identical to the app repository's version.
docs/architecture-proposal.md
